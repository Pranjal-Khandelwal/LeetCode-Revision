Approach-1 (Recursion)
T.C-O(2^n)
S.C-O(1)

class Solution {
public:
    int uniquePaths(int m, int n) {
    
        if(n==1 || m==1)
            return 1;
        
        return uniquePaths(m,n-1)+uniquePaths(m-1,n);
    }
};
