T.C-O(N^2)
S.C-O(1)

class Solution {
public:
    vector<int> findDuplicates(vector<int>& nums) {
        int n=nums.size();
        
        vector<int>res;
        
        for(int i=0;i<n;i++)
        {
            for(int j=i+1;j<n;j++)
            {
                if(nums[i]!=nums[j]){
                    continue;
                }else{
                    res.push_back(nums[i]);
                    break;
                }
            }
        }
        return res;
    }
};
